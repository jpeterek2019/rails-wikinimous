
<header>
<div class="d-flex justify-content-between p-3 mb-2 bg-light border-bottom">
  <p>'Wiki - Logo</p>
  <p><%= link_to "New article", new_article_path %></p>
</div>
</header>

<div class='container'>
  <% @articles.each do |article| %>
    <h2><%= link_to article.title, article_path(article) %></h2>
  <% end %>

</div>
